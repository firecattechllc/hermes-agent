{
  "schema_version": 1,
  "document_purpose": "Discovery-stage access and credential matrix for every Hermes add-on in the prerequisite program. No secret values are recorded anywhere in this file.",
  "retrieval_date": "2026-08-05",
  "addons": [
    {
      "id": "paperclip",
      "required_account": "None to evaluate the source; a self-hosted instance requires no external account (local-loopback mode by default)",
      "required_credential": "None for local-loopback self-host; per-agent API keys generated locally once self-hosted",
      "required_hardware": "Any machine that can run Node.js + PostgreSQL (fits Titan or Mac profile in existing fleet placement)",
      "required_network_access": "None externally required for local-loopback mode; Tailnet binding optional for remote access",
      "required_repository": "https://github.com/paperclipai/paperclip (not yet added as a repository or dependency anywhere in this codebase)",
      "license_status": "MIT — compatible with private/internal use, no action needed",
      "deployment_target": "Undecided — candidate: Titan (per docs/beta/post-phase9/FLEET_PLACEMENT.md worker-hosting guidance)",
      "currently_available": false,
      "currently_missing": "Operator decision to proceed; a pinned commit/release Stage 1 registry entry; a Stage 2+ live implementation (currently only the disabled Stage 4 adapter exists)",
      "operator_action": "Confirm github.com/paperclipai/paperclip is the intended project; decide self-host location (Titan recommended); do not install until Stage 2 (worker contract) is live-wired",
      "security_risk": "Moderate — MCP tool gateway and agent workspace execution model must be intermediated by Hermes admission, never exposed directly",
      "release_dependency": "hermes-paperclip-integration; requires hermes-addon-foundation merged first"
    },
    {
      "id": "buzz",
      "required_account": "None for self-host; a buzz.xyz account only if using Block's hosted relay option",
      "required_credential": "None for self-host (Nostr keypairs are generated locally, not issued by a third party); Block account credential only if hosted option chosen",
      "required_hardware": "Machine that can run Docker Compose stack: relay (Rust/Axum), Postgres, Redis, S3/MinIO",
      "required_network_access": "Outbound only for self-host; inbound WebSocket/REST port (default 3000) if any remote client (human or agent) needs to reach it",
      "required_repository": "https://github.com/block/buzz (not yet added as a repository or dependency anywhere in this codebase)",
      "license_status": "Apache-2.0 — compatible with private/internal use, no action needed",
      "deployment_target": "Undecided — candidate: Titan or Prime, pending resource sizing review (Redis + Postgres + S3/MinIO footprint)",
      "currently_available": false,
      "currently_missing": "Operator decision on self-host vs. Block-hosted relay; a pinned commit/release Stage 1 registry entry; live Stage 5/6 wiring (currently only the disabled adapters exist)",
      "operator_action": "Decide self-host vs. buzz.xyz hosted relay; confirm \"Buzznode\" is treated as an internal worker-host classification rather than a separate upstream to source (see upstream registry)",
      "security_risk": "High — buzz-cli exposes direct shell execution and workflow-run capability to agents; must never be reachable by an agent without passing through Hermes admission first",
      "release_dependency": "hermes-buzz-integration; requires hermes-addon-foundation and hermes-paperclip-integration correlation-ID design merged first"
    },
    {
      "id": "supabase",
      "required_account": "Existing Supabase account under org \"firecattechllc\" (confirmed present)",
      "required_credential": "Supabase CLI access token (for `supabase login`); project anon/public key and service-role key for later client wiring (never recorded here)",
      "required_hardware": "None additional — hosted service",
      "required_network_access": "Outbound HTTPS to supabase.com / *.supabase.co",
      "required_repository": "N/A (hosted service, not a repository dependency); client SDK to be added when wired",
      "license_status": "N/A — commercial hosted service, account already exists",
      "deployment_target": "sigil-v3.8-supabase-platform",
      "currently_available": true,
      "currently_missing": "Local Supabase CLI (not installed); CLI authentication/link (not established); a documented dev/prod project split (only one project exists, currently INACTIVE/paused); migrations; RLS policy coverage; Auth configuration; backup/monitoring plan",
      "operator_action": "Install Supabase CLI, run `supabase login`, run `supabase link --project-ref qsfyoikpqxtutrcdbkhm` (or restore the project first if it should remain the target), then decide whether a second project is needed for dev/prod separation before any migration is written",
      "security_risk": "Moderate — service-role key must never enter client bundles, logs, prompts, or this repository; RLS must be enabled and reviewed before any public read/write path exists",
      "release_dependency": "sigil-v3.8-supabase-platform; independent of the Stage 0-12 ecosystem adapter program"
    },
    {
      "id": "hydra-live",
      "required_account": "N/A — private infrastructure, existing Tailscale identity firecat.techllc@",
      "required_credential": "SSH access (posture not yet re-verified in this pass); Tailscale membership (already established)",
      "required_hardware": "N/A — already provisioned VMware ARM64 Ubuntu VM",
      "required_network_access": "Tailscale overlay network (hydra-live node currently offline)",
      "required_repository": "N/A — deployment target, not a source repository",
      "license_status": "N/A",
      "deployment_target": "Hydra Live (application/runtime boundary per docs/architecture/hydra-ecosystem/CANONICAL_ARCHITECTURE.md)",
      "currently_available": false,
      "currently_missing": "Node is offline (Tailscale last-seen ~52 minutes before this discovery pass); fleet heartbeat service was failed at last discovery; duplicate Snap Tailscale unresolved; port 3130 owner unidentified",
      "operator_action": "Bring the Hydra Live VM back online (VMware guest power-on) so Tailscale reconnects; only then run the existing governed read-only discovery/diagnose tooling in docs/operations/hydra-live/OPERATOR_PLAYBOOK.md before any repair proposal",
      "security_risk": "Moderate — ports 3000/3099/3130 were observed listening on all interfaces at last discovery with unreviewed exposure intent",
      "release_dependency": "hermes-agent-reach / fleet placement work; independent of add-on upstream integrations"
    },
    {
      "id": "alexa-plus",
      "required_account": "Amazon developer account (developer.amazon.com) at minimum; Amazon partner relationship required specifically for Alexa+ for Builders (MCP path)",
      "required_credential": "OAuth 2.1 authorization-server credentials with PKCE S256 support (for the MCP add-on path); Lambda/HTTPS endpoint credentials (for classic Smart Home Skills path)",
      "required_hardware": "A publicly reachable HTTPS endpoint (Lambda or self-hosted) — cannot be purely internal/private for either integration route",
      "required_network_access": "Public inbound HTTPS endpoint required by Amazon; conflicts with a fully private Hermes deployment goal",
      "required_repository": "N/A",
      "license_status": "Amazon developer program terms; certification required before publication (either route)",
      "deployment_target": "feature/alexa-integration",
      "currently_available": false,
      "currently_missing": "No Amazon developer account confirmed for this program; no partner relationship for Alexa+ for Builders; no public endpoint provisioned",
      "operator_action": "Decide whether the classic self-serve Smart Home Skills route (device-control only, fully documented) or applying for Alexa+ for Builders partner access (broader MCP integration, gated, requires a public endpoint) is worth pursuing; if neither is acceptable given the private-network requirement, document Alexa+ as deferred",
      "security_risk": "High — any Alexa integration requires exposing something to the public internet, and voice-command actions need an explicit phone-approval gate for anything sensitive regardless of which route is chosen",
      "release_dependency": "feature/alexa-integration; independent, can be deferred indefinitely without blocking other add-ons"
    },
    {
      "id": "obsidian",
      "required_account": "None — local application",
      "required_credential": "None",
      "required_hardware": "None additional",
      "required_network_access": "None (fully local, read-only integration)",
      "required_repository": "N/A — existing bundled Hermes skill already implements filesystem-first vault access",
      "license_status": "Obsidian app is proprietary freeware; vault contents are plain Markdown owned by the user",
      "deployment_target": "feature/obsidian-knowledge-integration",
      "currently_available": false,
      "currently_missing": "Obsidian.app is not installed on this machine; no OBSIDIAN_VAULT_PATH is configured specifically for Hermes; no vault has been designated for Hermes use",
      "operator_action": "Install Obsidian (optional, only needed for human-side editing) and explicitly set OBSIDIAN_VAULT_PATH in ~/.hermes/.env to the vault the operator wants Hermes to read — until this is set, Hermes must not search for or guess a vault",
      "security_risk": "Low if scoped to a single explicitly-configured, non-home-directory vault path with read-only access; High if broad home-directory or auto-discovery access were ever granted (explicitly disallowed by this task)",
      "release_dependency": "feature/obsidian-knowledge-integration; independent, low-risk, can proceed as soon as vault path is configured"
    },
    {
      "id": "fred",
      "required_account": "Free FRED account at fredaccount.stlouisfed.org",
      "required_credential": "FRED_API_KEY (32-character key) — not currently set in this environment",
      "required_hardware": "None",
      "required_network_access": "Outbound HTTPS to fred.stlouisfed.org/api",
      "required_repository": "N/A — thin REST client to be added under Sigil",
      "license_status": "Free public API; FRED Terms of Use apply (attribution, no resale as a competing raw-data feed)",
      "deployment_target": "sigil-v4.1-strategy-engine",
      "currently_available": false,
      "currently_missing": "FRED_API_KEY environment variable (unset); client module; caching layer; retry/failure policy; test fakes",
      "operator_action": "Create a free FRED account, generate an API key, set FRED_API_KEY in the environment used by Sigil (never commit it); confirm with the operator before any live call is made",
      "security_risk": "Low — read-only public economic data API; key exposure risk is limited to quota abuse, not data breach",
      "release_dependency": "sigil-v4.1-strategy-engine; independent of the Stage 0-12 ecosystem adapter program"
    },
    {
      "id": "self-evolution",
      "required_account": "N/A — internal capability",
      "required_credential": "N/A — explicitly denied credential access by design",
      "required_hardware": "Same as existing Sigil backend (no new hardware)",
      "required_network_access": "None required for proposal-only operation",
      "required_repository": "N/A — already implemented in this repository (apps/sigil/src/sigil/self_evolution.py)",
      "license_status": "N/A — internal code",
      "deployment_target": "hermes-self-evolution-safety",
      "currently_available": true,
      "currently_missing": "A dedicated safety review document tying the existing framework/policy docs together with an explicit recommendation (delivered by this program — see docs/security/HERMES_SELF_EVOLUTION_SAFETY_REVIEW.md); no execution authority exists and none is recommended yet",
      "operator_action": "Review docs/security/HERMES_SELF_EVOLUTION_SAFETY_REVIEW.md and confirm the proposal-only recommendation before any future stage considers execution authority",
      "security_risk": "Currently minimal (no execution authority exists); would become high without independent review, budgets, and rollback gates if execution authority is ever proposed",
      "release_dependency": "hermes-self-evolution-safety; documentation-only, no code change required to merge"
    }
  ]
}
